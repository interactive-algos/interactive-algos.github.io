

function Display(canvas, bgCanvas, map, robot, filter) {
	this.canvas = canvas;
	this.bgCanvas = bgCanvas;
	this.map = map;
	this.robot = robot;
	this.filter = filter;
}

Display.prototype.update = function () {

}

Display.prototype.updateCanvas() {
	
}
